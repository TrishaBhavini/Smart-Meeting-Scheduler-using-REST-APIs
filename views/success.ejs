<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Meeting Scheduled</title>
  <link rel="stylesheet" href="styles/success.css" />
</head>
<body>
  <div class="container">
    <div class="checkmark-circle">
      <svg class="checkmark" viewBox="0 0 52 52">
        <path d="M14 27l7 7 17-17" />
      </svg>
    </div>

    <h1>Meeting Successfully Scheduled!</h1>

    <p>Thank you, <strong><%= name %></strong>.</p>
    <p>Your meeting is scheduled for <strong><%= date %></strong> at <strong><%= time %></strong> in <strong><%= mode %></strong> mode.</p>

    <% if (mode === "online") { %>
      <p>🔗 <strong>Meeting ID:</strong> <%= locals.meetingID %></p>
      <p>🔗 <strong>Join URL:</strong> <a href="<%= locals.joinURL %>" target="_blank"><%= locals.joinURL %></a></p>
      <p>🔐 <strong>Passcode:</strong> <%= locals.passcode %></p>
    <% } else { %>
      <p>📍 Your offline meeting is confirmed.</p>
    <% } %>
    <p>📧 A confirmation email has been sent to <strong><%= email %></strong>.</p>
    <a href="/" class="button-link">Schedule Another Meeting</a>
  </div>
</body>
</html>

